spring:
  application:
    name: spring-cloud-config-client
  cloud:
    config:
      uri: http://localhost:8888
      name: web-admin-feign-bootstrap
      label: master
      profile: dev

server:
  port: 8899

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/